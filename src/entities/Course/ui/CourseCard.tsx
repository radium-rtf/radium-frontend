import React from 'react';
import { Card } from '@/shared';
import { CourseResponseDto } from '@/entities/Course';

export const CourseCard = (course: CourseResponseDto) => {
  return <Card></Card>;
};
