export const publicRoutes: string[] = [];

export const authRoutes: string[] = ['/signin', '/signup', '/signup/verify'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT_URL = '/';
